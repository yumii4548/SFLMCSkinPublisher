# 审核邮件发送程序使用说明

通过网易企业邮箱 SMTP 发送皮肤审核结果通知邮件。纯 Python 标准库实现，**无需安装任何第三方库**。

- **立即发送**：走 SMTP，直接发出。
- **定时发送（`--time`）**：注册 Windows 一次性计划任务（Windows 自带 `schtasks`），到点自动 SMTP 发送，终端即可关闭。

## 所需文件与环境

- 只需要 2 个文件：`email_sender.py` 和 `config.py`
- 需要 Python 3.8+（建议 3.10+），Windows 系统
- 换电脑运行：把这 2 个文件拷到一个文件夹，`python email_sender.py ...` 即可

## 快速开始

### 审核通过（最简）

不发 `--result` 时默认为「同意」：

```bash
python email_sender.py --to 2176179242@qq.com --name "作品名字" --author "作者名字"
```

### 审核通过 + 指定发布账号

`--account chym` 会自动显示为账号名「沧海月明」：

```bash
python email_sender.py --to 2176179242@qq.com --name "作品名字" --author "作者名字" --account chym
```

### 审核不通过

```bash
python email_sender.py --to 2176179242@qq.com --name "作品名字" --author "作者名字" --result 不同意
```

### 带附件 + 抄送

```bash
python email_sender.py --to 2176179242@qq.com --name "作品名字" --author "作者名字" --image output/final.png --cc a@swiftfishlab.cn
```

### 定时发送（--time，自动到点发送）

`--time` 为未来时间时，程序默认（`--scheduler auto`）走 **网易网页版邮箱「云端定时发信」**：
自动打开浏览器登录网易邮箱、填好邮件、开启定时发信并点发送，邮件存到**网易云端**，
到点由网易服务器自动发出 —— **电脑关机也不影响**。排好后立即返回，不用保持终端/浏览器打开：

```bash
python email_sender.py --to 2176179242@qq.com --name "作品名字" --author "作者名字" --time "2026/8/14 17:00:00"
```

#### 首次使用云端定时发信（只需准备一次）

1. **保存登录态**：`python email_sender.py --add-mail-account` —— 浏览器打开网易邮箱，手动登录（含验证码）后关闭窗口，登录态会保存到 `mail_profile/`。
2. **校准选择器**：`python email_sender.py --inspect-mail` —— 手动写一封带「定时发信」的信，把关键控件（写信按钮/收件人/主题/正文/附件/定时发信/发送）的真实选择器记回 `config.py` 的 `MAIL_SELECTORS`。

#### 调度方式（--scheduler）

| 取值 | 行为 |
|------|------|
| `auto`（默认） | 优先云端定时发信；云端**提交前**失败 → 回退注册 Windows 一次性计划任务；已点发送但未确认是否排定 → **不回退**（防双发），请到网页版「定时发信」列表核对 |
| `webmail` | 只走云端定时发信，失败不发送 |
| `task` | 强制旧的 Windows 计划任务方式（电脑需在到点开机） |

也可用环境变量 `set MAIL_SCHEDULER_MODE=task` 强制旧的计划任务方式。

> 若指定的时间已过，会提示并改为 SMTP 立即发送。
> 云端定时发信时间精确到分钟（秒>0 会自动顺延到下一分钟）；使用 `task` 方式时到点电脑需开机。

## 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| `--to` | ✅ | 收件人邮箱，多个用英文逗号分隔，如 `a@qq.com,b@qq.com` |
| `--name` | ✅ | 作品 / 资源名称（会出现在邮件主题和正文里） |
| `--author` | 否 | 作者名字，默认空 |
| `--result` | 否 | 审核结果：`同意`（默认）或 `不同意` |
| `--account` | 否 | 发布账号标识，如 `chym` → 显示「沧海月明」；未指定显示「极鱼社O组」 |
| `--publish-time` | 否 | 邮件正文里的上架时间，格式 `2026-08-08 17:50`；不填时定时发送取 `--time` 计划发送时间，立即发送取当前时间 |
| `--time` | 否 | 定时发送时间，格式 `2026/8/14 17:00:00`；未来时间 → 默认走网易云端定时发信（关机也能发），立即返回；不填立即发送 |
| `--scheduler` | 否 | 定时调度方式：`auto`（默认，云端优先失败回退计划任务）/ `webmail`（强制云端）/ `task`（强制 Windows 计划任务） |
| `--image` | 否 | 最终渲染图路径，作为附件发送 |
| `--cc` | 否 | 抄送邮箱，多个用逗号分隔 |
| `--add-mail-account` | 否 | 首次登录网易邮箱并保存网页端登录态（云端定时发信前置步骤） |
| `--inspect-mail` | 否 | 校准网易邮箱写信/定时发信选择器 |

## 邮件内容说明

- **审核通过**：含上架时间、发布账号（上架时间默认 = `--time` 计划发送时间，立即发送 = 当前时间）
- **审核不通过**：含理由「已有相似皮肤，需添加更多原创元素」
- 邮件底部会自动内嵌 slogan 宣传图（`config.py` 里的 `PROMO_IMAGE_PATH`），图片不存在时自动跳过，不影响发送
- 定时发送与立即发送内容完全一致（同一套模板 + 附件 + 内嵌图）

## 常见配置（config.py）

| 配置项 | 说明 |
|--------|------|
| `SMTP_SERVER` / `SMTP_PORT` / `SMTP_USE_SSL` | SMTP 服务器，默认网易企业邮箱 587 STARTTLS |
| `SENDER_EMAIL` / `SENDER_PASSWORD` | 发件账号密码，仅从环境变量读取（仓库不含密码） |
| `REVIEWER_EMAIL` | 默认收件人；不设时**必须传 `--to`**，否则会跳过发送 |
| `ACCOUNT_DISPLAY_NAMES` | 账号标识 → 显示名映射，新账号在此添加 |
| `PROMO_IMAGE_PATH` | slogan 宣传图路径（换电脑需改成本机路径） |

### 环境变量覆盖

```bash
set SENDER_EMAIL=你的邮箱
set SENDER_PASSWORD=你的密码
set REVIEWER_EMAIL=默认收件人
```

## 返回码

- 退出码 `0`：成功（立即发送 = 发送成功；`--time` 定时 = 计划任务注册成功）
- 退出码 `1`：失败（SMTP 登录失败 / 计划任务注册失败等，控制台会打印原因）

## 注意事项

1. `--to` 不传且未设 `REVIEWER_EMAIL` 时，程序会跳过发送并提示
2. `--time` 定时：默认走网易云端定时发信，关机也能发（需先 `--add-mail-account` 保存登录态并校准选择器）；使用 `--scheduler task` 旧方式时到点电脑需开机（睡眠/关机不补发）。云端未配置且回退计划任务也失败时，程序会回退为前台等待到点发送（Ctrl+C 可中断）
3. 发件密码仅从环境变量读取，请勿把密码提交到仓库
